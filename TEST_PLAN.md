# ロボボン 検査・受入計画

- 文書版: 0.1
- 作成日: 2026-08-16
- 状態: 計画を追加した段階。ここに書いた検査や利用者確認を実施済みとは扱わない
- 対象: `chameleonjp-lab/robobon`

この文書は、コード検査、iPhoneの手動確認、初見の利用者確認、公開前の独立確認を同じ合格として混ぜないために使います。数値を満たしていても、未実施を合格へ繰り上げません。

## 1. 判定の状態

各行へ次のどれか一つを記録します。

| 状態 | 意味 |
|---|---|
| 未実施 | まだ確認していない。合格数へ入れない |
| 実施中 | 記録はあるが、必要な条件がそろっていない |
| 合格 | 条件、対象SHA、実施者、日時、残る危険が記録されている |
| 不合格 | 条件を満たさない。修正回数と次の判断を記録する |
| 保留 | 権利、名称、端末、Previewなど、判断に必要な前提がない |

P0〜P6はActions成果物の検査までとし、GitHub Pagesを有効化しません。P7で名称、独自性、素材、性能、手動受入、完成条件をそろえ、所有者が明示承認した後だけ公開を検討します。

## 2. プルリクエストの自動検査

現在のCIは、プルリクエストごとに次を一つの成果物へまとめます。

| 検査 | 合格条件 | 記録先 |
|---|---|---|
| TypeScript型検査 | `tsc --noEmit`成功 | Actionsのjob |
| 単体検査 | 全Vitest成功 | Actionsのjob |
| 本番ビルド | Vite build成功 | Actionsのjob |
| Pages基準パス | `/robobon/`、必須素材、外部scriptなし | `check:build` |
| 由来確認 | `review-manifest.json`とActions SummaryのSHA・成果物名が一致 | ZIPとSummary |
| 成果物保存 | `robobon-dist-<workflow SHA>`が取得できる | Actions artifact |

目標時間は10分以内です。CIが失敗した場合は、失敗したSHA、seedや状態識別値（出力される場合）、失敗job、再実行結果をPRへ残します。Actions成果物は実機Previewを保証しないため、ZIPの取得だけでP3〜P4の手動受入を合格にしません。

夜間の10,000戦、広いブラウザ組合せ、画像比較、長時間性能検査はまだ構成されていません。構成されるまで、実施済みの検査として説明しません。月間のActions予算を超えそうな場合は、必須のプルリクエスト検査を優先し、夜間検査を止めた理由を記録します。

## 3. P4の利用者確認

P4-20、P4-21、P4-22は別の確認として集計します。同じ人の再確認は新しい参加者へ数えず、再確認行へ記録します。

| 確認 | 対象 | 最低人数 | 主な合格条件 | 記録 |
|---|---|---:|---|---|
| P4-20 分析画面 | 初見確認者 | 5人を目標 | 事実・不足・実験案を区別し、自動変更がない | `docs/P4_MANUAL_ACCEPTANCE.md` |
| P4-21 任務導線 | P4-22とは別の初見確認者 | 5人を目標 | 入力30秒、導入戦60秒、問いと次の操作を説明 | `docs/P4_MANUAL_ACCEPTANCE.md` |
| P4-22 初見確認 | P4-21とは別の初見確認者 | 5人を必須 | 4項目それぞれ4/5以上、未達は最大2回改善 | `docs/P4_MANUAL_ACCEPTANCE.md` |

5人未満は不足データです。途中離脱、同意撤回、欠測、通信やPreviewの失敗は成功へ数えませんが、理由を匿名メモへ残します。壊れた記録、重複ID、形式不正の入力は全体を拒否し、人数へ混ぜません。

P7の公開候補では、P4とは別に新規の最低20人を目標にします。端末（iPhone 17 Pro / iPhone 11 Proなど）、Safari版、経験の有無を分けて集計し、同じ人の再テストを新規人数へ加えません。20人を集められない場合は一般公開へ進まず、限定公開と不足データを明示します。

## 4. 記録する項目と保持

外部送信は行いません。記録するのは、匿名ID、実施日、端末とOS、Safari版、対象SHA、Previewの期限、所要時間、観察、同意、欠測・途中離脱の理由だけです。氏名、連絡先、自由記述から個人を特定できる情報は保存しません。

匿名ID、メモ、真偽値は`src/first-use.ts`の検査を通します。匿名IDは小文字英数字・ハイフン・下線で32文字以内、メモは500文字以内とし、説明可否と再戦は真偽値または`null`にします。

手動確認の生記録は判定日から30日以内に削除し、集計人数、判定、修正回数、残る危険だけを計画記録へ残します。同意撤回があれば、その確認者の生記録を先に削除し、集計から除外したことを記録します。実際の保存場所と削除日は、手動受入表の判断欄へ追記します。

## 5. 不具合の重さ

| 区分 | 例 | 公開条件 |
|---|---|---|
| S0 | 保存破壊、安全上の問題、外部送信の意図しない発生 | 0件 |
| S1 | 進行不能、勝敗や再生結果の破損 | 0件 |
| S2 | 主要機能の不良だが回避策がある | 回避策、担当、修正予定を記録 |
| S3 | 文言、余白、軽微な見た目の問題 | 既知一覧へ記録 |

S0またはS1が一つでも残っていれば、P7公開候補を停止します。S2を残す場合も、利用者へ隠さず、回避策を手動受入表へ書きます。

## 6. 参照記録

- `docs/P4_MANUAL_ACCEPTANCE.md`: P4-20〜P4-22の手動記録
- `docs/P2-14_TEST_RECORD.md`: iPhoneの保存・書出し・読込み記録
- `docs/RUNBOOK_IOS.md`: PR、Actions成果物、復旧のiPhone手順
- `docs/NAME_REVIEW.md`: 名称調査の保留条件
- `docs/ORIGINALITY_REVIEW.md`: 独自性と類似回避の保留条件

この計画の未記入欄は、失敗ではなく「まだ判断できない」状態を示します。実施していない確認を、CI成功やコード完成だけで補いません。
